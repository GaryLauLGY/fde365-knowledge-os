---
name: kb-health
description: |
  检查六类资产库的配置、目录、来源、收件箱、内容阶段、运行状态和已安装 kb-*。默认只报告，确认后才修目录。触发方式：/kb-health、「知识库体检」「检查知识库」。
---

# 知识库体检

<!-- KB-OWNED-WORKFLOW: v1 -->

## 开始前

1. 从当前目录向上找到 `.kb/config.yaml`，按配置解析六类资产库。
2. 只读取本任务需要的文件，不跨知识库搜索。
3. 把库内事实、用户本轮信息、当前推断和未知项分开。
4. 原始材料不覆盖；删除、移动和批量写入先确认。

## 读取

- `.kb/config.yaml`
- 六类资产路径
- 收件箱和内容阶段
- `.kb` 运行目录
- AGENTS.md、CLAUDE.md 和已安装 kb-* 清单

## 必须保留的能力

- 核对知识库配置与实际目录
- 核对 Agent 规则文件、Skill 和项目状态
- 发现路径、数据、版本和状态冲突
- 先报告，确认后再修正

## 执行

1. 验证配置字段、路径边界和目录可读性。
2. 统计六类资产的文件数、来源覆盖和最近更新。
3. 对照 AGENTS.md、CLAUDE.md、Skill 目录和实际项目状态，找路径、版本、数字和状态冲突。
4. 检查待处理材料、重复入库、内容阶段冲突和运行文件越界。
5. 把问题分为阻塞、要处理和提醒，只给出有证据的问题。

## 交付

- 能否使用
- 问题路径和证据
- 影响
- 建议动作
- 等待确认的修正

每个关键判断附来源路径；没有来源的内容标为推断或待确认。

## 写回

- 默认不写；确认后只创建缺失空目录或修正明确的路径

## 停止条件

- 配置不存在时停止并询问根目录
- 业务事实冲突时不自动选边
- 不把空库当成损坏

## 接续

- 空库用 kb-interview
- 积压材料用 kb-ingest

来源沿革只用于追溯，不作为运行指令。需要追溯时读取 `references/source-map.md`。
