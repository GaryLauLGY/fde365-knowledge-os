# Changelog

## 1.2.0（待后端联调，未发布）

- 对照 kb-suite 当前提交合并账号与计费接口 UI，固定 api.ipzsk.com/v1。
- 34 个 FDE Skills 覆盖上游能力；移除 connect，保留既有更先进的入库/体检合同。
- Mac/Windows 安装器改为插件内邮箱登录，移除本机 Claude Code/Codex 连接及全局配置接管。
- 新增账号安全、错误处理、刷新并发与安装隔离测试。后端服务代码未随上游提供，尚未真实联调。

## [1.1.2] - 2026-08-30

### Added

- 原始材料列表新增单项和批量删除；二次确认后同时把选中收件记录与 `original_file` 指向的原始文件移入回收站，正式资产、处理记录和 Agent 对话保持不变。
- Agent 回答使用 Obsidian 原生 Markdown 渲染，并在工作台重绘和异步内容更新后恢复滚动位置。

### Changed

- 删除未注册的旧版 Dashboard、Inbox、Projects、Agents、Analytics 等并行前端；当前七个工作台视图拆入独立模块，旧主题样式同步清理。
- 内容生产收敛为选题、草稿、待审核、待发布、已发布五阶段闭环；发布数据改为单独上传分析。
- 一键安装、macOS、Windows 和插件运行时继续固定使用 FDE365 中转站；Claude 使用 `https://api.fde365.ai`，Codex 与插件使用 `https://api.fde365.ai/v1`，下载和自动更新使用 `https://fdekb.garylau.ai`。
- Windows 安装器的 Codex 备用包升级到官方 `0.151.0`，继续经 FDE365 下载中转并校验 SHA-256。

### Fixed

- 修复 `/fde-ingest` 仅生成分流预览便提前标记处理完成的问题；现在必须经过用户确认、全部入库步骤完成和真实写入验证后才会结案。
- 等待确认的材料按钮改为“前往确认”，点击恢复原处理对话并预填“确认执行”；该阶段不再进入重复批量处理。
- 修复总览“当前入口”卡片在中等宽度下把 `/fde-start` 挤到第二行的问题；状态文案现在单行省略，操作按钮固定同排。
- 修复旧 Vault 中 `AGENTS.md`、`.fde/config.yaml` 与 `.kb` 并存造成的规则真源冲突；兼容迁移只更新已知旧默认值，保留 `.kb` 历史内容和自定义业务路径。
- 修复 `/fde-health` 把 README、分流预览、处理记录和已终结材料误计为待处理积压的问题；旧预览仅作审计记录，最终状态以原始材料和最新处理证据为准。
- 内容阶段推进弹窗中的条件改为问号状态，不再用勾选图标暗示系统已经完成检测。
- 修复删除确认弹窗的危险按钮在鼠标悬停或键盘聚焦时被主题样式覆盖、文字和背景一起消失的问题。

### Removed

- 移除待处理页的内置语音录制、系统听写入口和 AI 语音转写；音频仍可作为普通原始文件上传处理。

## [1.1.1] - 2026-08-26

### Added

- 待处理页支持拖入任意文件、多选与批量处理，并显示运行中、成功和失败状态。
- 新增 AI 语音快速记录，保留原音频和可编辑转写稿，由用户决定是否运行 `/fde-ingest`。
- Agent 支持“需要批准”与 YOLO 两种执行模式、可调节右侧面板宽度、命令补全与跨页面持久会话。

### Changed

- 录音、聊天、会议、图片和文档统一归入“待处理材料”；旧 `0-录音处理` 目录会无损迁移，重名内容不覆盖。
- `/fde-ingest` 将材料形式与六类资产归属分开，允许一份材料建议多个去向，并标记录音转写中的不确定信息。
- 待处理材料的后续确认与重新处理继续使用同一 Agent 对话，历史列表改为显示主要内容与主题。
- 个人资产统一采用“个人说明书”中性命名，保留说明书能力并定向迁移旧模板。

### Fixed

- 修复笔记本窄屏下流程卡片文字竖排、图标与文字重叠、六库信号条与名称重叠以及操作按钮文字对齐问题。
- 修复 Agent 对话文本无法选择复制、发送后输入框残留和已结案材料仍显示为待处理的问题。

## [1.1.0] - 2026-08-26

### Added

- 右侧工作区升级为真正的本地 Codex Agent：通过 app-server 复用会话、读取当前 Vault、运行 FDE Skills，并在需要额外权限时弹出用户确认。
- Agent 执行过程可向右侧面板回传本地工具状态，并保留跨工作台的会话 ID。

### Changed

- Agent 改为当前 Vault 独立 `CODEX_HOME`：插件自动生成 FDE365 API 配置，不接管 `~/.codex`，不影响本机 Codex App，也无需重新运行安装器。
- Token 仍只在 Obsidian 中填写并保存一份；只在启动隔离 Agent 子进程时临时传入，不写入隔离配置或用户级配置。
- macOS 与 Windows 安装器不再修改 Shell 或 Windows 用户环境变量。
- 插件设置页只保留 Token 和四个固定模型选项，移除 Temperature 与 Provider 相关可编辑项。

## [1.0.4] - 2026-08-26

### Fixed

- 右侧 AI 输入区的“选择笔记”改为可搜索的单选笔记入口，不再要求用户预先在编辑器中打开笔记。
- 明确选择主要笔记后，请求只使用该笔记及用户额外添加的上下文，不再静默混入另一个活动编辑器笔记。
- 右侧 AI 改为插件级独立会话；切换总览、六类资产、内容生产等中间工作台时，保留对话、草稿、标签页、生成状态和上下文选择。

## [1.0.3] - 2026-08-26

### Fixed

- 修复内容阶段推进和 FDE Skill 运行弹窗的主操作按钮文字不可见。
- 对话询问 FDE Skills、以及直接运行 Skill 时，由插件先在本地读取并附加已解析配置、能力目录与对应 `SKILL.md`，不再要求远端模型自行访问 Vault 文件系统。
- 移除已不存在的 Agent Center 引导，Skill 页面新增“查看运行记录”，统一跳转到右侧栏“历史”。

## [1.0.2] - 2026-08-26

- 插件自动更新改用 `fdekb.garylau.ai`，用户端不再直接连接 GitHub。
- macOS 与 Windows 安装器中的 Obsidian 下载、Windows Codex 备用下载改用同一国内更新服务。
- 新增 Cloudflare Worker 固定白名单代理；不提供任意 URL 转发，不接收或保存 Token。
- 保留现有更新等待行为，不增加 15 秒超时。

## [1.0.1] - 2026-08-26

### Changed

- 35 个 Skill 的目录、frontmatter、调用名、界面按钮和文档命令全部迁移到 `/fde-*` 命名空间。
- FDE Skills 模板版本升级到 `0.4.0`；自动更新后的旧 Vault 会以 create-only 方式补齐新目录，不删除可能已被用户修改的旧目录。

## [1.0.0] - 2026-08-25

### Added

- 新增 Mac 一键安装、更新与诊断脚本：自动从 Obsidian 官方发布地址下载安装应用，并部署 FDE365 工作台、插件、Token 和模型配置。
- 新增独立 Mac 安装包构建与静态审计，不包含 Obsidian 安装镜像、用户 `data.json` 或任何 Token。
- Mac 安装器扩展为三端一键配置：从官方地址安装 Claude Code 与 Codex，并将 Obsidian、Claude Code、Codex 同步到固定 FDE365 服务。
- 新增桌面 `FDE365 工具` 入口，可选择任意文件夹启动 Claude Code 或 Codex，并可一键打开知识库。
- 新增面向普通用户的图文安装说明，去除配置文件和开发术语。
- 新增隔离的 Mac 全流程模拟安装环境，可在假的 HOME 中验证安装、备份、三端配置、桌面入口和 Token 动态读取，不下载或启动真实应用。
- 新增由 `GaryLauLGY/fde365-knowledge-os` GitHub Releases 驱动的插件自动更新：启动后及每 6 小时检查，校验版本、仓库、文件白名单与 SHA-256 后安装，并提示重启 Obsidian。
- 新增标准 Obsidian Release 资产和 GitHub Actions 发布流程，Release 直接包含 `main.js`、`manifest.json`、`styles.css`、Logo、更新清单、完整 ZIP 与校验文件。
- 新增 Windows 一键安装、更新、诊断、官方应用下载与 PowerShell 5.1 自动化验收。

### Changed

- 插件内 Skills 产品名称统一为“FDE Skills”。

### Security

- 安装器会把 Claude Code、Codex 和 Shell 的 FDE365 连接项统一写好；Token 通过标准输入交给本地配置助手，只保存到新 Vault 的插件 `data.json`。
- Token 仍只保存在当前 Vault；Claude Code 使用 `apiKeyHelper`、Codex 使用 `auth.command` 在启动时动态读取，不在全局客户端配置中复制明文 Token。
- 同步客户端配置前自动备份 Claude Code、Codex 与 Shell 的原文件。
- 自动更新只允许替换五个插件运行资源，并在写入前保留本地备份；`data.json`、Token、知识笔记和其他 Vault 内容不在更新范围内。

### Fixed

- 修复 macOS 旧版 Bash 在中文提示符紧邻变量名时误判变量边界、导致安装/更新/诊断提前退出的问题。
- 右侧 AI 工作区在窄视图下改为覆盖在主内容上方并保持可见，不再被响应式规则直接隐藏。
- 新建资产弹窗的操作按钮使用独立文字标签和可见性保护，避免只显示图标没有文字。

## [0.6.0] - 2026-08-25

### Changed

- 插件 ID、npm 包名和独立发布包内目录统一为 `fde365-knowledge-os`，解压后不再出现旧品牌目录名。
- 蓝图 ID 与运行时 CSS 类名前缀统一迁移为 FDE365 命名，并在发布检查中禁止旧标识回流。

### Migration

- 本版本会被 Obsidian 识别为新的插件 ID，适合直接导入新仓库测试；旧 ID 下保存的 Token 与设置不会自动迁移。

## [0.5.1] - 2026-08-25

### Fixed

- 移除横版 FDE365 Logo 后方重复的文字品牌区，避免侧栏内文字被挤到内容区并截断。
- Logo 在侧栏内居中并允许按可用宽度缩小，保持完整比例且不溢出。

## [0.5.0] - 2026-08-25

### Changed

- 插件名称、界面、AI 助手、设置、新人指引和知识库模板品牌统一为 FDE365。
- 使用用户提供的 FDE365 横版 Logo，并生成透明底运行资源；深色主题自动反色保持可见。
- 新仓库默认创建 `FDE365知识库`；已有旧版知识库目录继续原地使用，不移动或覆盖用户内容。
- 独立发布包更名为 `FDE365-Knowledge-OS-Plugin-v<version>.zip`。

## [0.4.0] - 2026-08-25

### Added

- 新人指引升级为五步，新增购买 Token、打开 Token 设置和选择模型的完整教程。
- 模型配置限制为 `claude-fable-5`、`claude-opus-4-8`、`gpt-5.6-sol`、`gpt-5.6-luna` 四个选项。

### Changed

- AI 接入改为单一FDE365服务；服务地址固定在运行时，设置页不再显示或接受 Base URL。
- 凭据在全部用户界面统一称为 Token，并继续只保存在当前 Vault 的插件 `data.json`。
- 右侧 AI 面板的状态按钮改为直接打开 Token 与模型设置。

### Removed

- 移除 Codex CLI、Claude CLI、自定义 Base URL 和 Provider 切换入口及运行代码。

## [0.3.3] - 2026-08-22

### Fixed

- 中等宽度视图下，主入口按钮移到状态条第二行并限制在卡片内部，不再从右侧溢出。

## [0.3.2] - 2026-08-22

### Fixed

- 响应式布局改为按 Obsidian 当前插件视图宽度判断，打开原生文件树后也能正确降级布局。
- 可用宽度变小时，六库卡片先从三列切换为两列；更窄时收起 AI 辅助栏和项目内导航，避免文字、卡片与按钮挤压或溢出。
- 主内容区禁止意外横向滚动，窄视图保持单一纵向滚动。
- 单列布局中的六库与资产卡片改为横向铺满内容区，去除卡片内部过多的左右留白。

## [0.3.1] - 2026-08-22

### Added

- 首次启用后显示四步新人指引悬浮窗，介绍收集、六类资产、AI 协作和本地边界。
- 增加指引进度、上一步、下一步、跳过与关闭操作，并适配浅色、深色和窄窗口。
- 命令面板和插件设置新增“重新打开新人指引”入口。

### Changed

- 指引完成、跳过或关闭后在当前 Vault 记录引导版本，后续启动不再重复打扰。

## [0.3.0] - 2026-08-21

### Added

- 将右侧 AI 区重构为 FDE365 统一工作面，整合对话、FDE、Skills、历史、当前笔记、显式文件上下文和新对话。
- 增加 Provider 快速切换面板，支持已登录的 Codex CLI、Claude CLI 和当前 Vault 内的 OpenAI-compatible API。

### Changed

- 六类资产的全部筛选项改为安全的自适应网格，编号、名称和数量不再跨出按钮。
- 资产卡片的分类编号与名称之间增加固定间距。
- 用户可见文案全部以“FDE365”为主体，不展示 WIS 或 Claudian 品牌。

## [0.2.4] - 2026-08-21

### Changed

- 增加 FDE Skills 目录卡片的垂直空间，确保第二行中文说明与下边框保持安全间距。
- 统一 Skill 图标、两行文字和部署状态的垂直居中与行高。

## [0.2.3] - 2026-08-21

### Changed

- 修复 FDE Skills 页目录卡片被 Obsidian 原生按钮高度压缩，导致图标和说明跨出卡片的问题。
- Skill 卡片改为自适应高度，并统一图标、标题、说明和部署状态的安全边界。

## [0.2.2] - 2026-08-21

### Changed

- 所有可见的 WIS、SIX-ASSET 和 WORKSPACE 品牌文案改为FDE365中文体系。
- 修复右侧 Skill 快捷能力名称与说明跨出卡片边界的问题。
- 修复顶部“设置”文字被塞入图标按钮宽度而溢出的问题。
- 为插件设置页和高密度组件补充响应式换行、截断与最小宽度约束。

## [0.2.1] - 2026-08-21

### Changed

- 恢复FDE365原有的白底、浅蓝分区和蓝紫渐变视觉语言，移除插件组件中的系统灰色按钮底。
- 修复总览资产卡片和六类资产条目的内容挤压、遮挡与截断。
- 内容生产改为自适应 3×2 阶段布局，不再依赖横向滚动查看第六阶段。
- 收紧空状态、Skill、关系网络和体检组件的高度与间距，统一白色卡片和蓝色边框层级。

## [0.2.0] - 2026-08-21

### Changed

- 面板完全改为 FDE Skills 六类资产模型，不再使用 AI 技术、商业模式等旧 Knowledge-OS 主题分类。
- 总览改为个人说明书、产品、客户需求、素材案例、方法论、内容生产六库状态与来源质量。
- 内容项目改为选题、草稿、待审核、待发布、已发布、数据复盘六阶段流水线。
- AI 协作改为 35 个项目本地 `fde-*` Skills，Codex 从知识库根目录读取 `.agents/skills`。
- 资产网络改为六库真实链接矩阵，资产分析改为来源、未知项、版本、阶段冲突和 Skill 部署体检。
- AI 上下文限制在当前知识库内，不跨 Vault 读取其他项目笔记。

### Added

- `.fde/config.yaml` 运行时路径解析、项目级 `AGENTS.md` 和完整 FDE Skills 模板。
- 六库定向新建、待处理安全入库流程、内容阶段确认推进与知识质量自动化测试。

## [0.1.0] - 2026-08-21

### Added

- FDE365蓝白简约商务界面，以及六类资产知识驾驶舱。
- 插件首次启用时自动初始化 `FDE365知识库`，只创建缺失目录与文件。
- 本地 Codex CLI、本地 Claude CLI、自定义 OpenAI-compatible API 三种执行方式。
- 知识库检查/修复命令、版本记录、发布包校验与第三方许可说明。
